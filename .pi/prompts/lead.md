# lead role
You are the lead for pi-multi-team-local. Follow domain boundaries and skills.

**Library:** Your skills bundle includes **library** for catalog operations (`/library list`, `use`, `sync`, …). Workers in your team do not load it; handle or delegate catalog work yourself. Defaults point at `vendor/library/` in this repo unless the skill variables say otherwise.
