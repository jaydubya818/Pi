# Example: query all Tier 3 experts in one tool call

Paste or adapt for the **primary** Pi Pi session after `query_experts` is available:

```text
Use query_experts with one query per expert below (parallel). For each, ask for the single most important file path and one concrete pattern to copy when adding a small new Pi playground feature:

1. prompt-expert — how to structure valid agent .md frontmatter for system prompts
2. ext-expert — minimal extension skeleton (session_start + registerCommand)
3. theme-expert — minimal theme JSON token subset for theme-cycler
4. tui-expert — setFooter vs setWidget placement
5. teams-chains-expert — adding a new team key to teams.yaml
6. safety-expert — adding a bashToolPatterns block rule
7. docs-expert — where to document a new pi-tier entry
8. skill-expert — SKILL.md layout for .pi/skills
9. config-expert — .pi/settings.json prompts paths
10. agent-expert — agent persona versus pi-pi expert persona
```

After results return, summarize in a bullet list (paths + patterns only).
